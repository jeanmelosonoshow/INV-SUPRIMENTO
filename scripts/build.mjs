import fs from"node:fs";import path from"node:path";import process from"node:process";const root=process.cwd(),out=path.join(root,"dist"),files=["index.html","styles.css","app.js","config.js","manifest.webmanifest","favicon.svg","service-worker.js","src/inventory.js","data/products.json"],catalog=JSON.parse(fs.readFileSync(path.join(root,"data/products.json"),"utf8"));if(!Array.isArray(catalog)||!catalog.length)throw Error("O cadastro data/products.json está vazio.");fs.rmSync(out,{recursive:true,force:true});for(const file of files){const to=path.join(out,file);fs.mkdirSync(path.dirname(to),{recursive:true});fs.copyFileSync(path.join(root,file),to)}console.log(`Build concluído em dist com ${catalog.length} produtos.`);
